'use strict'

module.exports = {

    RSAEncrypt: function (input) {
        // Do encryption here.
        var encryptedByRSA = input + "Encrypted";
        return encryptedByRSA;
    },
    RSADecrypt: function (encryptedByRSA)
    {
        // Do decryption here.
        var output = encryptedByRSA + "Decrypted";
        return output;
    },
    GCD : function (first, second){
        
        // Do GCD calculation
        var result = 1;
        return result;
    }
  };
  